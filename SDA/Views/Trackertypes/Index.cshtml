@model IEnumerable<SDA.Models.Trackertype>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NoofSale)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tracker.Statuss)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NoofSale)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tracker.Statuss)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.typeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.typeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.typeID })
        </td>
    </tr>
}

</table>
