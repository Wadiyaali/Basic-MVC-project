@model IEnumerable<SDA.Models.Locat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LocDetails)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LocDetails)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LocID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LocID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LocID })
        </td>
    </tr>
}

</table>
